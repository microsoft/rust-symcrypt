name: Build Windows

on:
  push:
    branches: [ "main" ]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  FEATURES_TO_TEST: md5,sha1,pkcs1-encrypt-decrypt

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
        - target: x86_64-pc-windows-msvc
          os: windows-latest
          runs-on: windows-latest
          symcrypt: "https://github.com/microsoft/SymCrypt/releases/download/v103.8.0/symcrypt-windows-amd64-release-103.8.0-53be637d.zip"
          run-tests: true
        - target: aarch64-pc-windows-msvc
          os: windows-latest
          runs-on: windows-latest
          symcrypt: "https://github.com/microsoft/SymCrypt/releases/download/v103.8.0/symcrypt-windows-arm64-release-103.8.0-53be637d.zip"
          run-tests: false # Windows doesn't support ARM64 emulation
    runs-on: ${{ matrix.os }}
    name: ${{ matrix.target }}    
    env:
      CARGO_BUILD_TARGET: ${{ matrix.target }}

    steps:
    - uses: actions/checkout@v4 # Checks out SymCrypt based on Github submodule
      with:
        submodules: true

    # - uses: Swatinem/rust-cache@v2  # temporarily disabled to rule out stale cache
    
    # Install host architecture, required for cross-compilation since there is no arm64-windows-msvc runner
    - name: Install host target
      shell: pwsh
      run: |
        rustup target add ${{ matrix.target }}

    - name: Download SymCrypt and Set Environment Variables
      shell: pwsh
      run: |
          Invoke-WebRequest -Uri ${{ matrix.symcrypt }} -OutFile symcrypt.zip
          New-Item -ItemType Directory -Force -Path symcrypt
          Expand-Archive -Path symcrypt.zip -DestinationPath symcrypt
          echo "$env:GITHUB_WORKSPACE\symcrypt\dll" >> $env:GITHUB_PATH
          echo "SYMCRYPT_LIB_PATH=$env:GITHUB_WORKSPACE\symcrypt\dll" >> $env:GITHUB_ENV
          echo "PATH=$env:GITHUB_WORKSPACE\symcrypt\dll;$env:PATH" >> $env:GITHUB_ENV

    - name: Diagnostics
      if: matrix.run-tests
      shell: pwsh
      run: |
          Write-Output "=== Rust ==="
          rustc --version --verbose
          Write-Output "=== OS Version ==="
          [System.Environment]::OSVersion
          Write-Output "=== Downloaded DLL hash ==="
          Get-FileHash "$env:GITHUB_WORKSPACE\symcrypt\dll\symcrypt.dll" -Algorithm SHA256
          Write-Output "=== Other symcrypt.dll on system ==="
          Get-ChildItem -Path C:\ -Filter "symcrypt.dll" -Recurse -ErrorAction SilentlyContinue | Select-Object FullName, Length
          Write-Output "=== PATH ==="
          $env:PATH -split ';' | Select-Object -First 10
          Write-Output "=== VC Runtimes installed ==="
          Get-ChildItem "C:\Windows\System32\vcruntime*.dll" -ErrorAction SilentlyContinue | Select-Object Name, Length

    - name: Debug build
      run: cargo build --locked --verbose --target ${{ matrix.target }}

    - name: Release build
      run: cargo build --release --locked --verbose --target ${{ matrix.target }}

    - name: Run tests (Debug)
      if: matrix.run-tests
      run: cargo test --locked --verbose --all-features --target ${{ matrix.target }} -- --no-capture

    - name: Run tests (Release)
      if: matrix.run-tests
      run: cargo test --release --locked --verbose --all-features --target ${{ matrix.target }} -- --no-capture
